# 复制本文件为 .env 并填入真实值。
# .env 已被 .gitignore 忽略，不会进入 Git 仓库。
# 也可以用系统环境变量提供，二者等效。

DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# 可选：最大迭代轮数、单次模型调用超时（秒）
# AGENT_MAX_TURNS=30
# AGENT_TIMEOUT=120
